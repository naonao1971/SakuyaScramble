# NOUN RALLY ⌐◨-◨ (proto)

CNP11体を集める Rally-X 系の迷路ゲーム。咲耶スクランブル本編とは別ゲームだが、
**救出対象の11体は本編と同じキャラ・同じ絵を共有している**。

`rally/index.html` 単体で動く。ビルド不要。

## 遊び方

- 車は止まらない。**曲がる方向だけ決める**（進行方向が壁のときだけ中心で停止）
- 迷路に散らばった CNP 11体を全部回収するとクリア
- 赤い敵車に触れるとアウト
- `SMOKE` で敵をスピンさせられるが、燃料を多く食う
- 燃料が尽きてもアウト（＝実質の制限時間）

| 操作 | PC | スマホ |
| --- | --- | --- |
| 曲がる | 矢印 / WASD | 十字パッド、または画面スワイプ |
| 煙 | Space | SMOKE ボタン |

## CNP 11体

`index.html` の `CNP` 配列。`id` / 名前 / `hue` は本編（`../index.html` の
`cnpChars`）と**同一の正式データ**にしてある。

絵は `../assets/cnp/<id>.png` をそのまま読むので、本編側で画像を差し替えれば
ラリー側にも自動で反映される。画像が無いスロットは `drawCharPlaceholder()` が
描く色付きシルエット（「?」入り）に落ちる。1枚も無くても回収・当たり判定・
レーダーはすべて動く（本編と同じ方針）。

| id | 名前 | hue |
| --- | --- | --- |
| cnp_sotai_leelee | リーリー | 196 |
| cnp_sotai_mitama | ミタマ | 150 |
| cnp_sotai_narukami | ナルカミ | 46 |
| cnp_sotai_orochi | オロチ | 24 |
| cnp_sotai_luna | ルナ | 220 |
| cnp_sotai_yama | ヤマ | 92 |
| cnp_sotai_makami | マカミ | 322 |
| cnp_sotai_towa | トワ | 262 |
| cnp_sotai_setsuna | セツナ | 174 |
| cnp_sotai_ema | エマ | 292 |
| cnp_sotai_taruto | タルト | 352 |

`hue` はオーラ・名札・レーダー点の色に使う。11体は毎プレイ必ず1体ずつ配置
されるので（`NTARGET = CNP.length`）、レア度による出現差は今のところ無い。

## 描画まわりのメモ

- **自機は ⌐◨-◨ カー**。`drawNoggles()` は本編の同名関数と同じ作り。
  車体は進行方向に回すが、**ノグルズは回さない**（真上から見た車が曲がっても
  ⌐◨-◨ の顔が常に読めるようにするため）
- **名札**は幅を名前の実測に合わせてある。固定幅だと `TILE=24` より広がり、
  隣のマスのキャラの名札と重なって両方読めなくなる。それでもぶつかる場合は
  先に置いた名札を避けて逃がす。頭上に出すと HUD 帯（`y<22`）に潜る位置の
  ときは、最初から足元側に出して下へ逃がす
- キャラは暗い路面に沈むので、`hue` のオーラを下に敷いている

## 調整しやすい定数

```js
const TILE=24, COLS=33, ROWS=25;   // 迷路の寸法
const CHAR_SIZE=22;                // キャラの描画サイズ(px四方)
const CHAR_HIT=14;                 // 回収の当たり判定
```

敵は4台（`newGame()`）、岩は8個（`genMaze()`）、燃料は基本 1.3/秒 + 煙を出して
いる間 3.2/秒（`update()`）。スコアは n体目の回収で `100 + n*20` 点。11体
そろえると合計 2420 点。

## 残っていること

- スコアのランキング登録（本編は `apps-script.gs` で Google スプレッドシートに
  出しているが、ラリー側は `localStorage` のベストスコアのみ）
- レア度。本編には `CNP_TIERS` の5段階（得点・HP回復・出現率）があるが、
  ラリー側は11体とも同格
- クリア後の X シェア（本編の `buildShareCardCanvas()` 相当）
